class Route {
    constructor(id, name, description, kml, images) {
      this.id = id;
      this.name = name;
      this.description = description;
      this.kml = kml;
      this.images = images;
    }
  };

  module.exports = Route;
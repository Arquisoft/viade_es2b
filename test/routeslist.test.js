import React from 'react';
import { render, cleanup } from 'react-testing-library';
/*
import { HashRouter as Router } from 'react-router-dom';
import RouteList from '../src/containers/Home/components/RouteList.js';
import RoutesService from './__mocks__/RoutesService.js'
import '@testing-library/react';
*/

test('Arreglo momentaneo', () => {
    expect(true);
  })

/*
describe('RoutesList', () => {
    afterAll(cleanup);

    var routesService = new RoutesService();

    const { container } = render(
        <Router>
            <RouteList routes = {routesService.getRoutes()}/>
        </Router>
    );

    it('should have same routes as array in service', () => {
        expect(queryAllByTestId('routeElement')).toHaveLength(routesService.getRoutes().length);
    });
});
*/